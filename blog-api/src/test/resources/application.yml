spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      fail-on-emptybeans: false

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
